<VirtualHost *:80>
        ServerAdmin webmaster@localhost
        ServerName kurir.dev
        DocumentRoot /var/www/html/web/kurir/public

        <Directory />
                Options FollowSymLinks
                AllowOverride None
        </Directory>

        <Directory /var/www/html/web/kurir>
                Options FollowSymLinks
                AllowOverride All
                Order allow,deny
                allow from all
        </Directory>


        ErrorLog /var/log/apache2/kurir.error.log

        # Possible values include: debug, info, notice, warn, error, crit, alert, emerg.
        LogLevel warn

        SetEnvIfNoCase ^X-HTTPS$ .+ HTTP_X-HTTPS
        CustomLog /var/log/apache2/kurir.access.log combined env=!HTTP_X-HTTPS
        CustomLog /var/log/apache2/ssl.kurir.access.log combined env=HTTP_X-HTTPS

        ServerSignature Off
        AllowEncodedSlashes On

</VirtualHost>